#! /bin/bash

# Arg 1: project dir
# Arg 2: analysis id 

# Probably should make cghub url a variable

# Download bamfile
#rm -rf $mnt_dir/$1/$2 2> /dev/null
#mkdir $mnt_dir/$1/$2 2> /dev/null
#rm $cache_dir/$2* > /dev/null

gtdownload -c $cgkey -d https://cghub.ucsc.edu/cghub/data/analysis/download/$2 -p ~/${output_dir}



