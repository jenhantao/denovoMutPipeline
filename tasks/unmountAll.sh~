#! /bin/bash

# Arg 1: project dir
# Arg 2: analysis uid list

# Mount bamfile

# FIXME: Don't hardcode

for name in $(cat $2)
do
    export UUID=$(echo ${name##*/})
    fusermount -u $mnt_dir/$1/$UUID
done
